DELETE FROM План_выплат
WHERE ID_Займа IN (
    SELECT ID_Займа FROM Документ_займа
    WHERE ID_Клиента IN (SELECT ID_Клиента FROM Клиент WHERE Паспорт_серия = '9999')
);
DELETE FROM Документ_займа
WHERE ID_Клиента IN (SELECT ID_Клиента FROM Клиент WHERE Паспорт_серия = '9999');
DELETE FROM Платёжный_документ WHERE Номер_квитанции LIKE 'TEST-PAY-%';
DELETE FROM Клиент WHERE Паспорт_серия = '9999';

-- Клиент А — для Т1А, Т1Б, Т2А, Т2Б, Т2В
INSERT INTO Клиент (Фамилия, Имя, Дата_рождения, Паспорт_серия, Паспорт_номер)
VALUES ('Тестов', 'Алексей', '1990-01-01', '9999', '000001');

-- Клиент Б — для Т1В
INSERT INTO Клиент (Фамилия, Имя, Дата_рождения, Паспорт_серия, Паспорт_номер)
VALUES ('Тестов', 'Борис', '1990-01-01', '9999', '000002');

-- Отделения в базе
SELECT о.ID_Отделения, то.Название, то.Макс_сумма
FROM Отделение о JOIN Тип_отделения то ON о.ID_Типа = то.ID_Типа
ORDER BY то.Макс_сумма;

-- Т1А: займ 50 000 руб. в Малое отделение (лимит 100 000)

INSERT INTO Документ_займа
    (Дата_выдачи, Сумма_займа, Тип_выплат, Количество_дней, Крайняя_дата_возврата,
     Процентная_ставка_день, Пени_процент_день, ID_Клиента, ID_Отделения, ID_Бухгалтера)
SELECT CURRENT_DATE, 50000, 'По частям', 30, CURRENT_DATE+30, 1.5, 0.5,
    (SELECT ID_Клиента FROM Клиент WHERE Паспорт_серия = '9999' AND Паспорт_номер = '000001'),
    о.ID_Отделения,
    (SELECT ID_Сотрудника FROM Сотрудник WHERE ID_Отделения = о.ID_Отделения LIMIT 1)
FROM Отделение о JOIN Тип_отделения то ON о.ID_Типа = то.ID_Типа
WHERE то.Название = 'Малое' LIMIT 1;

-- Т1Б: займ 100 001 руб. в Малое отделение (лимит 100 000)

INSERT INTO Документ_займа
    (Дата_выдачи, Сумма_займа, Тип_выплат, Количество_дней, Крайняя_дата_возврата,
     Процентная_ставка_день, Пени_процент_день, ID_Клиента, ID_Отделения, ID_Бухгалтера)
SELECT CURRENT_DATE, 100001, 'По частям', 30, CURRENT_DATE+30, 1.5, 0.5,
    (SELECT ID_Клиента FROM Клиент WHERE Паспорт_серия = '9999' AND Паспорт_номер = '000001'),
    о.ID_Отделения,
    (SELECT ID_Сотрудника FROM Сотрудник WHERE ID_Отделения = о.ID_Отделения LIMIT 1)
FROM Отделение о JOIN Тип_отделения то ON о.ID_Типа = то.ID_Типа
WHERE то.Название = 'Малое' LIMIT 1;

-- Т1В: клиент Б берёт займы в 3 отделениях, 4-й должен упасть по сумме

INSERT INTO Документ_займа
    (Дата_выдачи, Сумма_займа, Тип_выплат, Количество_дней, Крайняя_дата_возврата,
     Процентная_ставка_день, Пени_процент_день, ID_Клиента, ID_Отделения, ID_Бухгалтера)
SELECT CURRENT_DATE, то.Макс_сумма, 'По частям', 365, CURRENT_DATE+365, 1.5, 0.5,
    (SELECT ID_Клиента FROM Клиент WHERE Паспорт_серия = '9999' AND Паспорт_номер = '000002'),
    о.ID_Отделения,
    (SELECT ID_Сотрудника FROM Сотрудник WHERE ID_Отделения = о.ID_Отделения LIMIT 1)
FROM Отделение о JOIN Тип_отделения то ON о.ID_Типа = то.ID_Типа
WHERE то.Название = 'Малое' LIMIT 1;

INSERT INTO Документ_займа
    (Дата_выдачи, Сумма_займа, Тип_выплат, Количество_дней, Крайняя_дата_возврата,
     Процентная_ставка_день, Пени_процент_день, ID_Клиента, ID_Отделения, ID_Бухгалтера)
SELECT CURRENT_DATE, то.Макс_сумма, 'По частям', 365, CURRENT_DATE+365, 1.5, 0.5,
    (SELECT ID_Клиента FROM Клиент WHERE Паспорт_серия = '9999' AND Паспорт_номер = '000002'),
    о.ID_Отделения,
    (SELECT ID_Сотрудника FROM Сотрудник WHERE ID_Отделения = о.ID_Отделения LIMIT 1)
FROM Отделение о JOIN Тип_отделения то ON о.ID_Типа = то.ID_Типа
WHERE то.Название = 'Среднее' LIMIT 1;

INSERT INTO Документ_займа
    (Дата_выдачи, Сумма_займа, Тип_выплат, Количество_дней, Крайняя_дата_возврата,
     Процентная_ставка_день, Пени_процент_день, ID_Клиента, ID_Отделения, ID_Бухгалтера)
SELECT CURRENT_DATE, то.Макс_сумма, 'По частям', 365, CURRENT_DATE+365, 1.5, 0.5,
    (SELECT ID_Клиента FROM Клиент WHERE Паспорт_серия = '9999' AND Паспорт_номер = '000002'),
    о.ID_Отделения,
    (SELECT ID_Сотрудника FROM Сотрудник WHERE ID_Отделения = о.ID_Отделения LIMIT 1)
FROM Отделение о JOIN Тип_отделения то ON о.ID_Типа = то.ID_Типа
WHERE то.Название = 'Крупное' LIMIT 1;

INSERT INTO Документ_займа
    (Дата_выдачи, Сумма_займа, Тип_выплат, Количество_дней, Крайняя_дата_возврата,
     Процентная_ставка_день, Пени_процент_день, ID_Клиента, ID_Отделения, ID_Бухгалтера)
SELECT CURRENT_DATE, то.Макс_сумма, 'По частям', 365, CURRENT_DATE+365, 1.5, 0.5,
    (SELECT ID_Клиента FROM Клиент WHERE Паспорт_серия = '9999' AND Паспорт_номер = '000002'),
    о.ID_Отделения,
    (SELECT ID_Сотрудника FROM Сотрудник WHERE ID_Отделения = о.ID_Отделения LIMIT 1)
FROM Отделение о JOIN Тип_отделения то ON о.ID_Типа = то.ID_Типа
WHERE то.Название = 'Крупное' LIMIT 1;


-- Т2А: оплачиваем первый из двух пунктов плана

INSERT INTO План_выплат
    (Плановая_дата_оплаты, Остаток_погашения_основы, Остаток_погашения_процентов,
     ID_Займа, ID_Бухгалтера, ID_Типа_начисления)
SELECT CURRENT_DATE+30, 5000, 0,
    з.ID_Займа, з.ID_Бухгалтера,
    (SELECT ID_Типа_начисления FROM Тип_начисления LIMIT 1)
FROM Документ_займа з
WHERE з.ID_Клиента = (SELECT ID_Клиента FROM Клиент WHERE Паспорт_серия = '9999' AND Паспорт_номер = '000001')
LIMIT 1;

INSERT INTO План_выплат
    (Плановая_дата_оплаты, Остаток_погашения_основы, Остаток_погашения_процентов,
     ID_Займа, ID_Бухгалтера, ID_Типа_начисления)
SELECT CURRENT_DATE+60, 5000, 0,
    з.ID_Займа, з.ID_Бухгалтера,
    (SELECT ID_Типа_начисления FROM Тип_начисления LIMIT 1)
FROM Документ_займа з
WHERE з.ID_Клиента = (SELECT ID_Клиента FROM Клиент WHERE Паспорт_серия = '9999' AND Паспорт_номер = '000001')
LIMIT 1;

INSERT INTO Платёжный_документ (Номер_квитанции, Назначение_платежа, Внесенная_сумма, Способ_оплаты, ID_Клиента, ID_Кассира)
VALUES (
    'TEST-PAY-T2AB', 
    'Оплата 10000 для двух пунктов',
    10000, 
    'Наличные', 
    (SELECT ID_Клиента FROM Клиент WHERE Паспорт_серия = '9999' AND Паспорт_номер = '000001'),
    (SELECT ID_Сотрудника FROM Сотрудник LIMIT 1)
);

UPDATE План_выплат
SET ID_Платежа = (SELECT ID_Платежа FROM Платёжный_документ WHERE Номер_квитанции = 'TEST-PAY-T2AB')
WHERE ID_Плана = (
    SELECT MIN(п.ID_Плана)
    FROM План_выплат п
    JOIN Документ_займа з ON з.ID_Займа = п.ID_Займа
    WHERE з.ID_Клиента = (SELECT ID_Клиента FROM Клиент WHERE Паспорт_серия = '9999' AND Паспорт_номер = '000001')
      AND п.ID_Платежа IS NULL
);

-- Т2Б: оплачиваем последний пункт, заём закрывается

UPDATE План_выплат
SET ID_Платежа = (SELECT ID_Платежа FROM Платёжный_документ WHERE Номер_квитанции = 'TEST-PAY-T2AB')
WHERE ID_Плана = (
    SELECT MIN(п.ID_Плана)
    FROM План_выплат п
    JOIN Документ_займа з ON з.ID_Займа = п.ID_Займа
    WHERE з.ID_Клиента = (SELECT ID_Клиента FROM Клиент WHERE Паспорт_серия = '9999' AND Паспорт_номер = '000001')
      AND п.ID_Платежа IS NULL
);

SELECT з.ID_Займа, з.Текущий_статус
FROM Документ_займа з
WHERE з.ID_Клиента = (SELECT ID_Клиента FROM Клиент WHERE Паспорт_серия = '9999' AND Паспорт_номер = '000001');

-- Т2В: займ 18 000, пункт 1 (9 000) проходит, пункт 2 (9 001) блокируется

INSERT INTO Документ_займа
    (Дата_выдачи, Сумма_займа, Тип_выплат, Количество_дней, Крайняя_дата_возврата,
     Процентная_ставка_день, Пени_процент_день, ID_Клиента, ID_Отделения, ID_Бухгалтера)
SELECT CURRENT_DATE, 18000, 'По частям', 60, CURRENT_DATE+60, 1.0, 0.5,
    (SELECT ID_Клиента FROM Клиент WHERE Паспорт_серия = '9999' AND Паспорт_номер = '000001'),
    о.ID_Отделения,
    (SELECT ID_Сотрудника FROM Сотрудник WHERE ID_Отделения = о.ID_Отделения LIMIT 1)
FROM Отделение о JOIN Тип_отделения то ON о.ID_Типа = то.ID_Типа
WHERE то.Название = 'Крупное' LIMIT 1;

INSERT INTO План_выплат
    (Плановая_дата_оплаты, Остаток_погашения_основы, Остаток_погашения_процентов,
     ID_Займа, ID_Бухгалтера, ID_Типа_начисления)
SELECT CURRENT_DATE+30, 9000, 0,
    з.ID_Займа, з.ID_Бухгалтера,
    (SELECT ID_Типа_начисления FROM Тип_начисления LIMIT 1)
FROM Документ_займа з
WHERE з.ID_Клиента = (SELECT ID_Клиента FROM Клиент WHERE Паспорт_серия = '9999' AND Паспорт_номер = '000001')
ORDER BY з.ID_Займа DESC LIMIT 1;

INSERT INTO План_выплат
    (Плановая_дата_оплаты, Остаток_погашения_основы, Остаток_погашения_процентов,
     ID_Займа, ID_Бухгалтера, ID_Типа_начисления)
SELECT CURRENT_DATE+60, 9001, 0,
    з.ID_Займа, з.ID_Бухгалтера,
    (SELECT ID_Типа_начисления FROM Тип_начисления LIMIT 1)
FROM Документ_займа з
WHERE з.ID_Клиента = (SELECT ID_Клиента FROM Клиент WHERE Паспорт_серия = '9999' AND Паспорт_номер = '000001')
ORDER BY з.ID_Займа DESC LIMIT 1;

INSERT INTO Платёжный_документ (Номер_квитанции, Назначение_платежа, Внесенная_сумма, Способ_оплаты, ID_Клиента, ID_Кассира)
VALUES (
    'TEST-PAY-T2V', 
    'Оплата ровно 18000',
    18000, 
    'Наличные', 
    (SELECT ID_Клиента FROM Клиент WHERE Паспорт_серия = '9999' AND Паспорт_номер = '000001'),
    (SELECT ID_Сотрудника FROM Сотрудник LIMIT 1)
);

UPDATE План_выплат
SET ID_Платежа = (SELECT ID_Платежа FROM Платёжный_документ WHERE Номер_квитанции = 'TEST-PAY-T2V')
WHERE ID_Плана = (
    SELECT MIN(п.ID_Плана)
    FROM План_выплат п
    JOIN Документ_займа з ON з.ID_Займа = п.ID_Займа
    WHERE з.ID_Клиента = (SELECT ID_Клиента FROM Клиент WHERE Паспорт_серия = '9999' AND Паспорт_номер = '000001')
      AND з.Сумма_займа = 18000
      AND п.ID_Платежа IS NULL
);

UPDATE План_выплат
SET ID_Платежа = (SELECT ID_Платежа FROM Платёжный_документ WHERE Номер_квитанции = 'TEST-PAY-T2V')
WHERE ID_Плана = (
    SELECT MAX(п.ID_Плана)
    FROM План_выплат п
    JOIN Документ_займа з ON з.ID_Займа = п.ID_Займа
    WHERE з.ID_Клиента = (SELECT ID_Клиента FROM Клиент WHERE Паспорт_серия = '9999' AND Паспорт_номер = '000001')
      AND з.Сумма_займа = 18000
      AND п.ID_Платежа IS NULL
);